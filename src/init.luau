-- // MODULES // --
local QuillTypes = require("@self/QuillTypes")

-- // MAIN // --

local Module = {
    Number = require("@self/QuillNumber")
}

-- ## PRIVATE ## --

-- ## PUBLIC ## --

export type Module = typeof(Module)

return Module :: Module